import { SelectValuePipe } from './select-value.pipe';

describe('SelectValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectValuePipe();
    expect(pipe).toBeTruthy();
  });
});
